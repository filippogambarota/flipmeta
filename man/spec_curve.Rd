% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_curve.R
\name{spec_curve}
\alias{spec_curve}
\title{Plot a Specification Curve}
\usage{
spec_curve(x, coef = NULL, wrap_width = 15)
}
\arguments{
\item{x}{An object of class \code{fml}.}

\item{coef}{Optional character vector specifying the coefficient(s) to
include. If \code{NULL}, all coefficients are included.}

\item{wrap_width}{Positive integer indicating the maximum number of
characters used when wrapping specification labels. Defaults to \code{15}.}
}
\value{
A \code{patchwork} object containing the specification curve, the number
of observations or studies (\code{k}) for each specification, and the analytic
choices defining each specification.
}
\description{
Creates a specification curve plot for an object of class \code{fml}.
Specifications are ordered by their estimated effect and displayed together
with the number of observations or studies included in each specification
and the analytic choices defining each scenario.
}
\details{
Statistical significance is represented using three categories:
specifications that are not significant before multiple-testing correction,
specifications that are significant before but not after correction, and
specifications that remain significant after correction.

Specifications are ordered according to their estimated effect. Significance
is classified as follows:
\itemize{
\item \code{"Never significant"} when the unadjusted p-value is greater than 0.05;
\item \code{"Lost after correction"} when the unadjusted p-value is at most 0.05 but
the adjusted p-value is greater than 0.05;
\item \code{"Significant after correction"} when the adjusted p-value is at most 0.05.
}
}
\examples{
\dontrun{
spec_curve(x)
spec_curve(x, coef = "intrcpt")
spec_curve(x, wrap_width = 20)
}

}
