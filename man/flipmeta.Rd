% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flipmeta.R
\name{flipmeta}
\alias{flipmeta}
\title{Flipmeta score sign-flipping tests for metafor models}
\usage{
flipmeta(
  fit,
  B = 5000,
  flips = NULL,
  tested_coeffs = NULL,
  method = NULL,
  extra = NULL,
  progress = TRUE,
  tol = .Machine$double.eps^0.25,
  control = list(),
  id = NULL
)
}
\arguments{
\item{fit}{An object of class \code{"rma"} from \code{metafor::rma.uni()}, or a list of such objects.}

\item{B}{Positive integer giving the number of sign flips. When \code{flips} is
supplied, \code{B} is ignored and the effective number of flips is
\code{nrow(flips)}.}

\item{flips}{Optional numeric matrix of precomputed flips containing only
\code{-1} and \code{1}. Its first row must contain only \code{1}, representing the
observed statistic. For a list of models, column names are required and
must be unique observation identifiers.}

\item{tested_coeffs}{Coefficients to test. For a single model, a character
vector. For a list of models, either a character vector applied to every
model or a list with one character vector (or \code{NULL}) per model. A \code{NULL}
value tests all coefficients in the corresponding model.}

\item{method}{Optional heterogeneity estimator. One of \code{"REML"}, \code{"ML"},
\code{"DL"}, or \code{"EE"}. If \code{NULL}, the method stored in \code{fit} is used. \code{"EE"}
fits an equal-effects model with tau^2 fixed at 0.}

\item{extra}{Optional data frame of model-level variables added to the
\code{summary_table} when \code{fit} is a list of models.}

\item{progress}{Logical indicating whether to show progress for lists of models.}

\item{tol}{Positive finite numerical tolerance.}

\item{control}{Optional list of numerical controls. Currently supports
\code{tau2_interval}, a finite length-two numeric vector used as the search
interval for tau^2 under the null.}

\item{id}{Character scalar naming the observation identifier column in each
model's \code{fit$data}. It is required when \code{fit} is a list and ignored for a
single model. For model lists, the column must be present and contain
unique, non-missing identifiers in every model.}
}
\value{
For a single fitted model, an object of class \code{"fm"}. Its main components
are:
\itemize{
\item \code{summary_table}: one row per tested coefficient, containing the model
estimate, observed flip statistic, score statistic and raw p-value;
\item \code{Tspace}: a data frame with one row per sign flip and one column per
tested coefficient. The first row contains the observed statistics;
\item \code{scores}: a numeric matrix of observation-level score contributions, with
observations in rows and tested coefficients in columns;
\item \code{flips}: the sign-flip matrix used in the analysis;
\item \code{null_fits}: the fitted null model for each tested coefficient;
\item \code{rma}: the original or refitted \code{metafor} model.
}

For a list of fitted models, an object of classes \code{c("fml", "fm")}. Its
\code{summary_table} and \code{Tspace} combine results across models, \code{objects} contains
the individual \code{"fm"} objects, and \code{flips} is the common sign-flip matrix.
Within every element of \code{objects}, the rows of \code{scores} represent the same
common universe of identifiers and are ordered as \code{colnames(flips)}.
Observations absent from a particular model have score contribution zero.
}
\description{
Flipmeta score sign-flipping tests for metafor models
}
\details{
When several models are supplied, sign-flip columns are aligned using the
column named by \code{id}. This column should be created once in the complete
data set and retained in all data sets used to fit the models. The source
identifier must be globally unique before the data are filtered or
reordered. The \code{id} argument is not used for a single model because no
cross-model alignment is required.

If the input model was fitted with a fixed \code{tau2}, that value is retained in
every coefficient-specific null model when \code{method} is omitted or matches
the input model's method. Supplying a different \code{method} requests a refit and
does not retain the fixed value (\code{"EE"} fixes it at zero).

The standardized statistic is undefined when any flip-specific variance is
zero or numerically degenerate; in that case \code{flipmeta()} reports an error
instead of silently removing the affected flips.

For specifications obtained by selecting studies from a common master data
set, create the identifier before filtering and retain it in every model's
data. For example, use \code{flipmeta(fits, id = "study_id")}.
}
\examples{
dat <- data.frame(
    study_id = paste0("study", 1:8),
    yi = c(0.10, 0.35, -0.05, 0.42, 0.18, 0.51, 0.07, 0.29),
    vi = c(0.04, 0.06, 0.05, 0.08, 0.04, 0.07, 0.05, 0.06),
    dose = c(0, 1, 0, 1, 0, 1, 0, 1)
)

fit <- metafor::rma.uni(yi, vi, mods = ~ dose, data = dat)
result <- flipmeta(fit, B = 99, tested_coeffs = "dose")
result$summary_table

fits <- list(
    all_studies = fit,
    selected = metafor::rma.uni(
        yi,
        vi,
        mods = ~ dose,
        data = dat[dat$study_id != "study3", ]
    )
)
joined <- flipmeta(
    fits,
    B = 99,
    tested_coeffs = "dose",
    id = "study_id",
    progress = FALSE
)
joined$summary_table
joined$objects$selected$scores["study3", ]

}
